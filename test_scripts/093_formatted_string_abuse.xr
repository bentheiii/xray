struct A(i: int)

fn to_str(a: A)->A{
    a
}

fn add(a: A, s: str) -> A{
    a
}

fn add(s: str, a: A) -> A{
    a
}

fn eq(a: A, b: A) -> bool{
    a::i == b::i
}

fn main()->bool{
    let f: A = f"hi {A(15)} there";
    assert(f == A(15))
}